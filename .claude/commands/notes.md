---
description: Append a quick note to notes/notes.md, the dashboard's single running notes page
argument-hint: [what you want to remember]
---

Append the note below to `notes/notes.md`, under whichever heading it fits best (People / Process /
Business Environment / Loose ends — create the heading if it doesn't exist yet). This mirrors the
"Notes page" rule in `CLAUDE.md`: light-touch only — append the point as given, lightly deduplicating
against anything already there, without rewriting or reorganizing the rest of the file.

Don't fold this into `domains/` and don't ask clarifying questions unless the note is genuinely
ambiguous — just file it. After saving, confirm in one line what was added and under which heading.
This file backs the dashboard's Content → Notes view, but don't republish the dashboard automatically
— that's a separate step she'll ask for when she wants the view refreshed.

Note to add:
$ARGUMENTS
