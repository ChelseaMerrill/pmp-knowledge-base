# Reports

Dated weak-area snapshots go here as `weak-areas-YYYY-MM-DD.md`, generated whenever you ask Claude for
a progress or weak-areas report. Each one is a point-in-time read of `practice/attempts.log` — keep
old ones rather than overwriting, so you can see whether a weak domain is actually improving across
the 12 weeks.
